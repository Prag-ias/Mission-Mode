# dependencies
/node_modules

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# env files — .env.example is the template and stays tracked
.env*
!.env.example

# vercel
.vercel

# typescript
*.tsbuildinfo

.vercel
.env*

# playwright artifacts
/test-results/
/playwright-report/

# source papers — 295MB of scans, kept local, never committed.
# Originals live in "Mission 2027/PYQs"; ingestion reads them and commits
# only the extracted JSON.
/PYQs/

# rendered page images, regenerable from the PDFs with pdftoppm
/.ingest/

